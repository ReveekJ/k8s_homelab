{{- if .Values.ingress.enabled }}
{{- $host := .Values.ingress.host | default (printf "hermes.%s" (.Values.baseDomain | default "reveek-io.ru")) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hermes
  labels:
    app.kubernetes.io/name: hermes
  annotations:
    cert-manager.io/cluster-issuer: {{ .Values.ingress.clusterIssuer | quote }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  tls:
    - hosts:
        - {{ $host }}
      secretName: {{ .Values.ingress.tlsSecretName }}
  rules:
    - host: {{ $host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hermes
                port:
                  name: serve
{{- end }}
