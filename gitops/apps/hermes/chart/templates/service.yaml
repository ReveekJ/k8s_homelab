apiVersion: v1
kind: Service
metadata:
  name: hermes
  labels:
    app.kubernetes.io/name: hermes
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hermes
  ports:
    - name: serve
      port: {{ .Values.serve.port }}
      targetPort: serve
